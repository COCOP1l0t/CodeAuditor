{
  "name": "protocol-auditor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "protocol-auditor": "dist/main.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "test": "node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.114.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
